#!/bin/bash
# SpaceCat Local Testing Configuration - EXAMPLE TEMPLATE
# 
# DO NOT EDIT THIS FILE DIRECTLY!
# Run ./setup-spacecat-tools.sh to generate your personal spacecat-config.sh
#
# This file shows the expected format and is tracked in git as a reference.
# The actual spacecat-config.sh is user-specific and gitignored.

# Path to spacecat-content-scraper directory
export SPACECAT_SCRAPER_DIR="/path/to/spacecat-content-scraper"

# Path to spacecat-audit-worker directory
export SPACECAT_AUDIT_WORKER_DIR="/path/to/spacecat-audit-worker"

# Path to my-workspace-tools directory (where these scripts live)
export SPACECAT_TOOLS_DIR="/path/to/my-workspace-tools"

# Path to scraper's tmp folder (used by audit worker's mock S3 client)
export SPACECAT_SCRAPER_TMP_DIR="$SPACECAT_SCRAPER_DIR/tmp"

# Optional: Default SpaceCat API key (leave empty to prompt each time)
export SPACECAT_API_KEY=""

# ============================================================================
# To create your personal config:
#   ./setup-spacecat-tools.sh
#
# This will generate spacecat-config.sh with your actual paths.
# ============================================================================

